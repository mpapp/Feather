//
// Prefix header for all source files of the 'Cushion' target in the 'Cushion' project
//

#ifdef __OBJC__
    #import <Cocoa/Cocoa.h>

    #ifdef DEBUG
        #define MPLog(fmt, ...) NSLog(fmt, ##__VA_ARGS__)
    #else
        #define MPLog(...) do{}while(0) 
    #endif

#endif
